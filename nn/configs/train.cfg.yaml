train_config:
  learn_rate: !!float 1e-4
  epochs: &e 1000

  losses:
    - convergence
  
  lambdas:
    - [1.0, 1.0]

  warmups:
    - [0, 0]

  losses_kwargs:
    # convergence kwargs
    conv_sigma: 0.01
    reg_weight: !!float 1e-4